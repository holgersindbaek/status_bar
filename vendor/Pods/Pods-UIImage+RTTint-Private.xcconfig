#include "Pods-UIImage+RTTint.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/UIImage+RTTint" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/UIImage+RTTint"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}